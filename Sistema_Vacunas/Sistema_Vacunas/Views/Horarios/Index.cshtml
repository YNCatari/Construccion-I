@model List<Sistema_Vacunas.Models.Horario>
@{
    ViewBag.Title = "Index";
}

<!-- Page header -->
<dv class="full-box page-header">
    <h3 class="text-left">
        <a href="~/Horarios/Agregar/" class="btn btn-success">Nuevo Registro</a> <br />
        <i class="fas fa-clipboard-list fa-fw"></i> &nbsp; Administracion de Horarios 
    </h3>

</dv>

<!--CONTENT-->
<!-- Content here-->
<!-- Content here-->
<div class="container-fluid">
    @using (Html.BeginForm("Index", "Horarios", FormMethod.Post))
    {
    }
    <div class="table-responsive">
        <table id="datatable" class="display" style="width:100%">
            <thead>
                <tr class="text-center">

                    <th>#</th>
                    <th>HORARIOS AM</th>
                    <th>HORARIOS PM</th>
                    <th>ESTADO</th>
                    <th>OPCIONES</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr class="text-center">
                        <td>@(item.id_horario)</td>
                        <td>@(item.iniciohorario)</td>
                        <td>@(item.iniciofin)</td>
                        <td>
                            <a href="#" class="btn btn-danger">@(item.estado == "A" ? "Activo" : "Inactivo" )</a>
                        </td>
                        <td>
                            <a href="~/Horarios/Agregar/@item.id_horario" class="btn btn-primary">Actualizar</a>

                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
