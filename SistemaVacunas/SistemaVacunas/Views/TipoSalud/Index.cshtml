@model List<SistemaVacunas.Models.Tipo_Salud>
@{
    ViewBag.Title = "Index";
}

<!-- Page header -->
<dv class="full-box page-header">
    <h3 class="text-left">
        <a href="~/TipoSalud/Agregar/" class="btn btn-success">Nuevo Registro</a> <br />
        <i class="fas fa-clipboard-list fa-fw"></i> &nbsp; Administracion de Tipo de Centro de Salud
    </h3>

</dv>

<!--CONTENT-->
<!-- Content here-->
<!-- Content here-->
<div class="container-fluid">
    @using (Html.BeginForm("Index", "TipoSalud", FormMethod.Post))
    {
    }
    @if (TempData["AlertMessage"] != null)
    {
        <div class="alert alert-success">
            <strong>Success !</strong>@TempData["AlertMessage"]
        </div>
    }
    <div class="table-responsive">
        <table id="datatable" class="display" style="width:100%">
            <thead>
                <tr class="text-center">

                    <th>#</th>
                    <th>NOMBRE</th>
                    <th>DESCRIPCION</th>
                    <th>ESTADO</th>
                    <th>OPCIONES</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr class="text-center">
                        <td>@(item.Id_tiposalud)</td>
                        <td>@(item.Nombre)</td>
                        <td>@(item.Descripcion)</td>
                        <td>
                            <a href="#" class="btn btn-danger">@(item.Estado == "A" ? "Activo" : "Inactivo" )</a>
                        </td>
                        <td>
                            <a onclick="return confirm('Esta Seguro que desea Actualizar Datos del registro?')" href="~/TipoSalud/Agregar/@item.Id_tiposalud" class="btn btn-primary">Actualizar</a>

                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>



